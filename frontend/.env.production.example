# FitPilot 前端生产环境变量示例
# 复制为 .env.production 后修改；构建时生效（Vite 在 build 期内联 import.meta.env）
#
# 注意：docker-compose.prod.yml 中前端镜像默认通过 Dockerfile ARG VITE_API_BASE=/api 注入，
# 通常无需额外创建本文件；仅在本机手动 `pnpm build` 生产包时使用。

# API 基址：相对路径，走 nginx /api 反代（nginx 会剥离 /api 前缀转发到 backend:8000）
VITE_API_BASE=/api
